import type { Product } from "@/lib/enrich";

const mockItems: Product[] = [
    {
        name: "Sadece test maddesi",
        sources: [{ url: "https://example.com" }],
        specs: {},
    } as Product,
];
